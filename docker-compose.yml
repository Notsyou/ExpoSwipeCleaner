services:
  expo-swipe-cleaner:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
      - "19000:19000"
      - "19001:19001"
    network_mode: host
    restart: unless-stopped